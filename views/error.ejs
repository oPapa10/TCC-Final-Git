<!-- filepath: views/error.ejs -->
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Erro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container text-center mt-5">
        <h1 class="display-4 text-danger">Erro <%= error.status || 500 %></h1>
        <p class="lead"><%= message %></p>
        <% if (error.stack) { %>
            <pre class="text-start bg-white p-3 rounded border"><%= error.stack %></pre>
        <% } %>
        <a href="/" class="btn btn-primary mt-4">Voltar para o início</a>
    </div>
</body>
</html>