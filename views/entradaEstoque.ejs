<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Entrada de Estoque | Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/entradaEstoque.css">

    <style>
.bloco-estoque {
    border-radius: 18px;
    background: #fff;
    box-shadow: 0 8px 32px rgba(67, 97, 238, 0.10);
    margin-top: 30px;
}
.bloco-estoque .form-label {
    color: #4361ee;
    font-size: 1rem;
}
.bloco-estoque input, .bloco-estoque select {
    font-size: 1rem;
    border-radius: 10px;
}
.bloco-estoque .btn-success {
    font-size: 1.1rem;
    padding: 0.8rem 0;
}
</style>
</head>
<body>
    <div class="container">
        <div class="header-section mb-4">
            <div class="d-flex justify-content-start align-items-center mt-3 mb-2">
                <a href="/adm" class="btn btn-primary btn-voltar"><i class="fas fa-arrow-left"></i> Voltar</a>
            </div>
            <h2 class="text-center">Entrada e Alteração de Estoque</h2>
        </div>
        <% if (sucesso) { %>
            <div class="alert alert-success text-center">Estoque atualizado com sucesso!</div>
        <% } %>
        <div class="row justify-content-center">
            <div class="col-lg-7 col-md-9">
                <form id="estoqueForm" action="/entradaEstoque" method="POST" class="card p-4 mb-4 bloco-estoque shadow-lg">
                    <div class="text-center mb-4">
                        <span class="rounded-circle bg-success bg-opacity-10 p-3 mb-2 d-inline-block">
                            <i class="fas fa-box-open fa-2x text-success"></i>
                        </span>
                        <h4 class="mb-0 fw-bold text-success">Adicionar ou Alterar Estoque</h4>
                        <p class="text-muted mt-1 mb-0">Atualize o estoque dos produtos de forma simples e rápida.</p>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Pesquisar Produto</label>
                        <div class="row g-2">
                            <div class="col-md-6">
                                <label for="searchProduto" class="form-label">Por nome:</label>
                                <input type="text" class="form-control" id="searchProduto" placeholder="Digite parte do nome do produto...">
                            </div>
                            <div class="col-md-6">
                                <label for="searchProdutoId" class="form-label">Por ID:</label>
                                <input type="number" class="form-control" id="searchProdutoId" placeholder="Digite o ID do produto...">
                            </div>
                        </div>
                        <select class="form-select mt-2" id="produto" name="produto" required>
                            <option value="">Selecione o produto</option>
                            <% produtos.forEach(function(produto) { %>
                                <option value="<%= produto.ID %>"><%= produto.nome %> (ID: <%= produto.ID %>, Estoque atual: <%= produto.estoque %>)</option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="quantidade" class="form-label fw-semibold">Quantidade a adicionar</label>
                        <input type="number" class="form-control" id="quantidade" name="quantidade" min="1">
                    </div>
                    <div class="mb-3">
                        <label for="novoEstoque" class="form-label fw-semibold">Alterar estoque diretamente</label>
                        <input type="number" class="form-control" id="novoEstoque" name="novoEstoque" min="0">
                    </div>
                    <button type="submit" class="btn btn-success w-100 fw-bold"><i class="fas fa-save"></i> Salvar</button>
                </form>
            </div>
        </div>
        <div class="card p-4 mt-4">
            <h4 class="mb-3">Produtos e Estoque Atual</h4>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nome</th>
                        <th>Estoque</th>
                    </tr>
                </thead>
                <tbody>
                    <% produtos.forEach(function(produto) { %>
                        <tr>
                            <td><%= produto.ID %></td>
                            <td><%= produto.nome %></td>
                            <td><%= produto.estoque %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
    <script src="/javascripts/entradaEstoque.js"></script>
</body>
</html>
