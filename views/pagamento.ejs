<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Pagamento via PIX</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <link rel="stylesheet" href="/stylesheets/theme.css">
    <link rel="stylesheet" href="/stylesheets/menu-superior.css">
    <link rel="stylesheet" href="/stylesheets/footer.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        .pix-container {
            max-width: 420px;
            margin: 40px auto;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.08);
            padding: 2rem 2.5rem;
            text-align: center;
        }
        .pix-qrcode {
            margin: 1.5rem 0;
        }
        .pix-copiaecola {
            width: 100%;
            font-size: 1rem;
            padding: 0.5rem;
            margin-bottom: 1rem;
            background: #f8f9fa;
            border: 1px solid #ddd;
            border-radius: 8px;
        }
        .btn-confirmar {
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar', { usuario: usuario }) %>
    <main>
        <div class="pix-container">
            <h2 class="mb-3 text-primary"><i class="fa fa-qrcode me-2"></i>Pagamento via PIX</h2>
            <p class="mb-1">Valor a pagar:</p>
            <h3 class="mb-3 text-success">R$ <%= Number(valor).toLocaleString('pt-BR', {minimumFractionDigits:2}) %></h3>
            <div class="pix-qrcode">
                <img src="<%= qrCodeImage %>" alt="QR Code PIX" width="220" height="220">
            </div>
            <p class="mb-2">Escaneie o QR Code acima ou copie o código abaixo:</p>
            <textarea class="pix-copiaecola" readonly onclick="this.select()"><%= payload %></textarea>
            <form action="/pagamento/comprovante" method="POST" enctype="multipart/form-data">
                <input type="hidden" name="valor" value="<%= valor %>">
                <input type="hidden" name="descricao" value="<%= descricao %>">
                <div class="mb-3">
                    <label for="comprovante" class="form-label">Envie o comprovante do pagamento:</label>
                    <input type="file" class="form-control" name="comprovante" id="comprovante" accept="image/*,application/pdf" required>
                </div>
                <button type="submit" class="btn btn-success btn-confirmar w-100">
                    <i class="fa fa-check-circle me-2"></i>Confirmar Pagamento
                </button>
            </form>
            <p class="mt-3 text-muted">Após o envio do comprovante, seu pedido será processado.</p>
        </div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>