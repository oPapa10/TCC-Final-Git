<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Área do Administrador - Center Motos</title>
    <link rel="stylesheet" href="/stylesheets/adm.css">
    <link rel="stylesheet" href="/stylesheets/barra_rolagem.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* ===== ESTILOS APENAS PARA O BOTÃO DE SAIR ===== */
        .btn-logout {
            background: #5536f0;
            color: white;
            border: none;
            border-radius: 12px;
            padding: 12px 24px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-logout:active {
            transform: translateY(-1px);
            box-shadow: 0 4px 15px rgba(220, 53, 69, 0.4);
        }

        .btn-logout::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }
   
        .btn-logout i {
            font-size: 1.1rem;
            transition: transform 0.3s ease;
        }

        /* ===== RESPONSIVIDADE DO BOTÃO ===== */
        @media (max-width: 768px) {
            .btn-logout {
                width: 100%;
                max-width: 200px;
                padding: 10px 20px;
                font-size: 0.95rem;
            }
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <div class="logo">
            <h1>Center Motos</h1>
        </div>
        <div class="admin-header d-flex justify-content-between align-items-center">
            <h1>Área Administrativa</h1>
            <form action="/admin/logout" method="POST" style="margin:0;">
                <button type="submit" class="btn-logout">
                    <i class="fas fa-sign-out-alt"></i>
                    Sair do ADM
                </button>
            </form>
        </div>
        <div class="admin-main">
            <div class="admin-card">
                <div class="admin-card-icon">📦</div>
                <div class="admin-card-title">Entrada de Estoque</div>
                <button class="admin-card-button" onclick="window.location.href='/entradaEstoque'">Acessar</button>
            </div>
              <div class="admin-card">
                <div class="admin-card-icon">🧾</div>
                <div class="admin-card-title">Controle de Vendas</div>
                <button class="admin-card-button" onclick="window.location.href='/adm/vendas'">Acessar</button>
            </div>
            <div class="admin-card">
                <div class="admin-card-icon">👀</div>
                <div class="admin-card-title">Ver Produtos</div>
                <button class="admin-card-button" onclick="window.location.href='/seeProduto'">Acessar</button>
            </div>
            <div class="admin-card">
                <div class="admin-card-icon">🎉</div>
                <div class="admin-card-title">Cadastrar Promoção</div>
                <button class="admin-card-button" onclick="window.location.href='/cadastrarPromocao'">Acessar</button>
            </div>
            <div class="admin-card">
                <div class="admin-card-icon">🛒</div>
                <div class="admin-card-title">Cadastrar Item</div>
                <button class="admin-card-button" onclick="window.location.href='/createItens'">Acessar</button>
            </div>
            <div class="admin-card">
                <div class="admin-card-icon">📁</div>
                <div class="admin-card-title">Cadastrar Categoria</div>
                <button class="admin-card-button" onclick="window.location.href='/createCategoria'">Acessar</button>
            </div>
          
        </div>
        <div class="admin-footer"></div>
    </div>

    <script>
        // Efeito adicional de confirmação ao sair
        document.addEventListener('DOMContentLoaded', function() {
            const logoutBtn = document.querySelector('.btn-logout');
            const form = logoutBtn.closest('form');
            
            form.addEventListener('submit', function(e) {
                if (!confirm('Tem certeza que deseja sair da área administrativa?')) {
                    e.preventDefault();
                }
            });
        });

        
    </script>
</body>
</html>