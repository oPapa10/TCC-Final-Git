<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Produtos Cadastrados | Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 1300px;
            margin: 2rem auto;
            padding: 0 15px;
        }
        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #dee2e6;
        }
        .btn-voltar {
            background-color: #4361ee;
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 0.5rem 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
        }
        .btn-voltar:hover {
            background-color: #3a56d4;
            color: #fff;
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .btn-novo {
            background-color: #4cc9f0;
            border: none;
            border-radius: 8px;
            padding: 0.5rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-novo:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .table-container {
            max-height: calc(100vh - 200px);
            overflow-y: auto;
            margin-top: 1rem;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            background: white;
        }
        .table {
            width: 100%;
            margin-bottom: 0;
            background-color: white;
        }
        .table thead th {
            position: sticky;
            top: 0;
            background-color: #4361ee;
            color: #fff;
            z-index: 10;
            padding: 0.75rem 1rem;
            vertical-align: middle;
            font-size: 0.97rem;
            white-space: nowrap;
        }
        .table td {
            padding: 0.75rem 1rem;
            vertical-align: middle;
            font-size: 0.97rem;
            white-space: normal;
        }
        .fixed-column {
            position: sticky;
            left: 0;
            background-color: white;
            z-index: 5;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
        }
        .fixed-column-action {
            position: sticky;
            right: 0;
            background-color: white;
            z-index: 5;
            box-shadow: -2px 0 5px rgba(0,0,0,0.1);
        }
        .table th.fixed-column,
        .table th.fixed-column-action {
            z-index: 15; /* Maior que o z-index do thead */
        }
        .produto-img {
            width: 70px;
            height: 70px;
            object-fit: cover;
            border-radius: 10px;
            border: 2px solid #e0e0e0;
            background: #f8f9fa;
        }
        .action-btns {
            display: flex;
            gap: 0.5rem;
        }
        .action-btn {
            border: none;
            border-radius: 50%;
            width: 2.5rem;
            height: 2.5rem;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .action-btn.edit {
            background-color: #ffb70322;
            color: #ffb703;
        }
        .action-btn.delete {
            background-color: #f7258522;
            color: #f72585;
        }
        .action-btn:hover {
            transform: scale(1.1);
        }
        .edit:hover {
            background-color: #ffb703;
            color: white;
        }
        .delete:hover {
            background-color: #f72585;
            color: white;
        }
        .badge-categoria {
            background-color: #e0fbfc;
            color: #293241;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-weight: 500;
        }
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #6c757d;
        }
        .produto-details {
            font-size: 0.92rem;
            color: #555;
        }
        .produto-details span {
            display: inline-block;
            margin-right: 10px;
            margin-bottom: 2px;
            background: #f1f3f4;
            border-radius: 5px;
            padding: 2px 8px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header-section">
            <div class="d-flex align-items-center">
                <a href="/adm" class="btn-voltar">
                    <i class="fas fa-arrow-left"></i> Voltar
                </a>
                <h2 class="mb-0 ms-3">Produtos Cadastrados</h2>
            </div>
            <a href="/createItens" class="btn btn-novo">
                <i class="fas fa-plus me-2"></i> Novo Produto
            </a>
        </div>

        <% if (produtos.length > 0) { %>
            <div class="table-container">
                <table class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th class="fixed-column">Imagem</th>
                            <th>Nome</th>
                            <th>Valor</th>
                            <th>Categoria</th>
                            <th>Cor</th>
                            <th>Tamanho</th>
                            <th>Peso</th>
                            <th>Cilindrada</th>
                            <th>Potência</th>
                            <th>Tanque</th>
                            <th>Material</th>
                            <th>Proteção</th>
                            <th>Estoque</th>
                            <th>Descrição</th>
                            <th class="fixed-column-action">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% produtos.forEach(function(produto) { %>
                            <tr>
                                <td class="fixed-column">
                                    <% if (produto.imagem) { %>
                                        <img src="<%= produto.imagem %>" alt="Imagem do Produto" class="produto-img">
                                    <% } else { %>
                                        <span class="produto-img d-flex align-items-center justify-content-center" style="background:#e0e0e0;">
                                            <i class="fas fa-box-open fa-2x" style="color:#bbb;"></i>
                                        </span>
                                    <% } %>
                                </td>
                                <td><strong><%= produto.nome %></strong></td>
                                <td>R$ <%= produto.valor ? Number(produto.valor).toFixed(2).replace('.', ',') : '0,00' %></td>
                                <td>
                                    <span class="badge-categoria">
                                        <%= produto.categoria_nome || produto.categoria || produto.Categoria_ID %>
                                    </span>
                                </td>
                                <td><%= produto.cor %></td>
                                <td><%= produto.tamanho %></td>
                                <td><%= produto.peso %></td>
                                <td><%= produto.cilindrada %></td>
                                <td><%= produto.potencia %></td>
                                <td><%= produto.tanque %></td>
                                <td><%= produto.material %></td>
                                <td><%= produto.protecao %></td>
                                <td><%= produto.estoque %></td>
                                <td>
                                    <div class="produto-details">
                                        <span><%= produto.descricao %></span>
                                        <% if (produto.thumbnails) { %>
                                            <span>Thumbnails: <%= produto.thumbnails %></span>
                                        <% } %>
                                    </div>
                                </td>
                                <td class="fixed-column-action">
                                    <div class="action-btns">
                                        <a href="/produtos/editar/<%= produto.ID %>" class="action-btn edit" title="Editar">
                                            <i class="fas fa-pencil-alt"></i>
                                        </a>
                                        <form action="/produtos/excluir/<%= produto.ID %>" method="POST" style="display:inline;">
                                            <button type="submit" class="action-btn delete" title="Excluir" onclick="return confirm('Tem certeza que deseja excluir este produto?')">
                                                <i class="fas fa-trash-alt"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-box-open fa-3x mb-3" style="color: #dee2e6;"></i>
                <h4>Nenhum produto cadastrado</h4>
                <p class="mb-4">Você ainda não possui produtos cadastrados no sistema.</p>
                <a href="/createItens" class="btn btn-novo">
                    <i class="fas fa-plus me-2"></i> Adicionar Primeiro Produto
                </a>
            </div>
        <% } %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>